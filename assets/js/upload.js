require(["jquery", "jquery.ui.widget", "tmpl.min", "load-image.min", "canvas-to-blob.min"], function($) {
	/*
	$(function() {

		// DEFINIR CLASSE DE UPLOAD PARA FORMULÁRIO DE FOTOS
		$('#frm-upload-foto').fileupload({
			url : $(this).attr('acttion')
		});

		// UPLOAD DE FOTO EM CROSSO DOMAIN
		$('#frm-upload-foto').fileupload(
				'option',
				'redirect',
				window.location.href.replace(/\/[^\/]*$/,
						'/cors/result.html?%s'));
						
	});
*/
});